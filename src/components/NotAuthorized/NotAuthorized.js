import React from "react";

const NotAuthorized = () => {
  return (
    <div>
      <h1 className="center error">You are not authorized to view the page</h1>
    </div>
  );
};

export default NotAuthorized;
